const config = {
  // 启动的端口
  port: 3000,
  // 数据库的配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: '000520',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config

